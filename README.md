Format for `class_list.txt`:

```
student one
student two
student three
```

Format for `rm_pairs.txt` (ordered pairs that should be removed for the pool of possible pairs):

```
student one,student two
student three,student four
```
